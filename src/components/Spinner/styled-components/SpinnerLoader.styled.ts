import styled from 'styled-components/native';

export const WrapSpinnerLoad = styled.View`
  margin: 15px 0;
  display: flex;
  flex-flow: row nowrap;
  justify-content: center;
  align-items: center;
`;
