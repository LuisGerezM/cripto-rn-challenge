// export const toFixedCriptoNumber = (dataToFixed: number):number =>
//   dataToFixed.toFixed(2);

export const toFixedCriptoNumber = (dataToFixed: number): number =>
  Number(dataToFixed.toFixed(2));
